# SME AGENT

## Steps to run 

1.Install dependencies:

   ```bash
   pip install -r requirements.txt
   ```
2.Run server 
   ```bash
   uvicorn Home:app --reload 
   ```

## Local Host Swagger
    http://localhost:8000/docs